import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  paragraph: {
    marginVertical: 8,
    fontSize: 16,
    color: '#292929',
    marginHorizontal: 10
  }
});

export default styles;